"""Load-testing helpers (Phase 4.2).

Business logic for `agmind loadtest …` — keeps cli/ a thin wrapper. Currently the
k6-backed chat load test (`agmind.loadtest.k6`).
"""

from __future__ import annotations
