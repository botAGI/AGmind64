# AGmind agent-core (Agno) runtime deps. agno pinned (the load-bearing one); openai = the
# client OpenAILike uses for the llama.cpp endpoint; psycopg[binary]+sqlalchemy = PostgresDb
# session/memory; openinference-instrumentation-agno + OTel = traces to Phoenix (no heavy
# arize-phoenix client in the image).
agno==2.6.12
openai>=1.40,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
psycopg[binary]>=3.2,<4
sqlalchemy>=2.0,<3
openinference-instrumentation-agno>=0.1.30
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-http>=1.27,<2
