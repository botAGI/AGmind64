id: ubuntu-compose
name: Ubuntu Compose
status: supported
summary: Existing AGmind lane for one Ubuntu host configured by Ansible and rendered to Docker Compose.
runtime:
  kind: compose
  renderer: agmind render compose
  profiles:
    - core
    - rag
    - observability
provisioner:
  kind: none
configurator:
  kind: ansible
  inventory_source: static-inventory
  playbooks:
    - ansible/install.yml
storage_profile: local-paths
secrets_profile: env-files
verification:
  commands:
    - agmind render compose --profile core,rag,security --domain ci.example.com --output /tmp/agmind-ubuntu-compose.yml
    - docker compose --env-file /opt/agmind/.env -f /tmp/agmind-ubuntu-compose.yml config --quiet
