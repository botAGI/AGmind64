# AGmind Loki config (Phase H'.D).
# Single-instance, без object store (filesystem) — для home/semi-prod.
# Retention 14d (см. deep-dive 03 §7: ~50-70 GB disk budget).

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9095
  log_level: warn

common:
  # Persistence MUST live under the in-container mount /loki (host bind
  # /var/lib/agmind/loki:/loki). Any other in-container path is unmounted and
  # ephemeral — data would be lost on container recreate.
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: loki_index_
        period: 24h

limits_config:
  retention_period: 14d
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  allow_structured_metadata: true
  max_global_streams_per_user: 5000

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  delete_request_store: filesystem

ruler:
  storage:
    type: local
    local:
      # Loki 3.x mkdir's this on startup; /etc/loki is the :ro config mount (EROFS),
      # so it must point at the writable /loki data mount.
      directory: /loki/rules
  rule_path: /tmp/loki/rules
  ring:
    kvstore:
      store: inmemory

analytics:
  reporting_enabled: false
