---
# Host-level security: UFW (LAN-only firewall) + fail2ban + опц. Authelia.

- name: Install UFW + fail2ban
  ansible.builtin.apt:
    name:
      - ufw
      - fail2ban
    state: present

- name: Configure UFW defaults (deny incoming, allow outgoing)
  community.general.ufw:
    state: enabled
    direction: incoming
    policy: deny

- name: UFW allow outgoing
  community.general.ufw:
    direction: outgoing
    policy: allow

- name: UFW allow SSH (rate-limited)
  community.general.ufw:
    rule: limit
    port: ssh
    proto: tcp

- name: UFW allow HTTP/HTTPS (LAN only)
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
    src: 192.168.0.0/16
  loop:
    - "{{ agmind_proxy_http_port }}"
    - "{{ agmind_proxy_https_port }}"

- name: UFW allow mDNS
  community.general.ufw:
    rule: allow
    port: "5353"
    proto: udp
    # NOTE: do NOT set src to the multicast group 224.0.0.251 — an mDNS query's SOURCE is the
    # peer's unicast LAN address, so a src=multicast rule never matches and discovery silently
    # falls back to default-deny. Allow 5353/udp inbound (LAN-scoped by the interface).

- name: Enable fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    enabled: true
    state: started

- name: Configure fail2ban sshd jail
  ansible.builtin.copy:
    dest: /etc/fail2ban/jail.d/agmind-sshd.conf
    content: |
      [sshd]
      enabled = true
      port = ssh
      maxretry = 5
      findtime = 600
      bantime = 3600
    mode: "0644"
  notify: restart fail2ban
